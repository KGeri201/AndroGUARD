pages:
  stage: deploy
  environment: production
  script:
    - mkdir .public
    - cp -r pages/* .public
    - mv .public public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
